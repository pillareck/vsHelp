<UserControl x:Class="vsHelp.UI.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="32">
        <StackPanel MaxWidth="800" HorizontalAlignment="Left" Width="Auto">
            
            <!-- SECTION TITLE -->
            <TextBlock Text="Restauração de Backup" 
                       FontSize="22" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}" 
                       Margin="0,0,0,4"/>
            <TextBlock Text="Realize o restore de bases de dados MySQL ou arquivos de sistema de forma automatizada." 
                       FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" 
                       Margin="0,0,0,32"/>
            
            <!-- DRAG & DROP AREA / FILE SELECTION -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Arquivo de Backup" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border x:Name="DropArea" Style="{StaticResource ModernInputStyle}" Cursor="Hand">
                            <Grid>
                                <!-- Placeholder (Visible if NOT selected) -->
                                <TextBlock Text="Clique para selecionar ou arraste o arquivo .zip, .rar ou .sql..." 
                                           VerticalAlignment="Center" Margin="12,0"
                                           Foreground="{StaticResource TextMutedBrush}" FontSize="12.5">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFileSelected}" Value="False">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                
                                <!-- File Info (Visible if selected) -->
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFileSelected}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    
                                    <!-- Status Icon -->
                                    <Ellipse Width="8" Height="8" Margin="0,0,10,0">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill" Value="{StaticResource WarningBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsBackupValid}" Value="True">
                                                        <Setter Property="Fill" Value="{StaticResource SuccessBrush}"/>
                                                    </DataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsBackupValid}" Value="False"/>
                                                            <Condition Binding="{Binding IsValidating}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Fill" Value="{StaticResource ErrorBrush}"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>

                                    <TextBlock Text="{Binding SelectedFile.FileName}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold" FontSize="12.5"/>
                                    <TextBlock Text=" • " Foreground="{StaticResource TextMutedBrush}"/>
                                    <TextBlock Text="{Binding SelectedFile.FormattedSize}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11.5"/>
                                    <TextBlock Text=" • " Foreground="{StaticResource TextMutedBrush}"/>
                                    <TextBlock Text="{Binding SelectedFile.Extension}" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <Button Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}" 
                                Content="SELECIONAR" Margin="12,0,0,0"
                                Command="{Binding SelectFileCommand}"
                                IsEnabled="{Binding IsValidating, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- PROGRESS SECTION -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Progresso da Operação" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Right" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                    </Grid>
                    
                    <ProgressBar Style="{StaticResource ModernProgressStyle}" Value="{Binding OperationProgress}" Margin="0,0,0,8"/>
                </StackPanel>
            </Border>

            <!-- LOGS SECTION -->
            <Border Style="{StaticResource ModernCardStyle}" Height="220" VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Logs de Execução" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                    
                    <Border Grid.Row="1" Background="#0A000000" CornerRadius="4" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12" x:Name="LogScrollViewer">
                            <ItemsControl ItemsSource="{Binding Logs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Style="{StaticResource LogTextStyle}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>

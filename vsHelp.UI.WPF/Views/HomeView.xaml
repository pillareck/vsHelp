<UserControl x:Class="vsHelp.UI.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="32">
        <StackPanel MaxWidth="800" HorizontalAlignment="Left" Width="Auto">
            
            <!-- SECTION TITLE -->
            <TextBlock Text="Restauração de Backup" 
                       FontSize="22" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}" 
                       Margin="0,0,0,4"/>
            <TextBlock Text="Fluxo automatizado: Validação, Extração e Restauração MySQL." 
                       FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" 
                       Margin="0,0,0,32"/>
            
            <!-- CONFIGURATION AREA -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Servidor MySQL" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                        <Border Style="{StaticResource ModernInputStyle}">
                            <TextBox Text="{Binding DbServer, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="Transparent" BorderThickness="0" Foreground="White" 
                                     VerticalAlignment="Center" Margin="10,0" FontSize="13"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <TextBlock Text="Nome do Banco" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                        <Border Style="{StaticResource ModernInputStyle}">
                            <TextBox Text="{Binding DbName, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="Transparent" BorderThickness="0" Foreground="White" 
                                     VerticalAlignment="Center" Margin="10,0" FontSize="13"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ACTION AREA -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Ação" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                    <Button Style="{StaticResource PrimaryButtonStyle}" 
                            Content="SELECIONAR E RESTAURAR BACKUP" 
                            Command="{Binding SelectAndRestoreCommand}"
                            IsEnabled="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                </StackPanel>
            </Border>

            <!-- PROGRESS SECTION -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Progresso do Pipeline" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Right" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                    </Grid>
                    
                    <ProgressBar Style="{StaticResource ModernProgressStyle}" Value="{Binding OperationProgress}" Margin="0,0,0,8"/>
                </StackPanel>
            </Border>

            <!-- LOGS SECTION -->
            <Border Style="{StaticResource ModernCardStyle}" Height="220" VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Logs de Execução" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                    
                    <Border Grid.Row="1" Background="#0A000000" CornerRadius="4" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12" x:Name="LogScrollViewer">
                            <ItemsControl ItemsSource="{Binding Logs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Style="{StaticResource LogTextStyle}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
